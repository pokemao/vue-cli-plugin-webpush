{
  "name": "@didi/webpush-sdk",
  "private": true,
  "scripts": {
    "preserve": "dotenv -c development -- node genTurboJsonScript.js",
    "serve": "turbo run serve",
    "prebuild": "dotenv -c production -- node genTurboJsonScript.js",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20"
  }
}
